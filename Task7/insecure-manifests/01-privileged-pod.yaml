apiVersion: v1
kind: Pod
metadata:
  name: pod-privileged
  namespace: audit-zone
  labels:
    app: task7-insecure
    violation: privileged
spec:
  containers:
    - name: nginx
      image: nginx:1.25-alpine
      securityContext:
        privileged: true
